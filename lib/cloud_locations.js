function getCloudLocations() {
    return [
            {'lon': 1, 'lat': 1},
            {'lon': 2, 'lat': 1},
            {'lon': 3, 'lat': 1},
            {'lon': 4, 'lat': 1}
        ];
}

module.exports = {
    
    getCloudLocations: getCloudLocations
    
};
